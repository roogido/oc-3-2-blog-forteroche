Prérequis : 
	- Création de la database blog_forteroche :
		CREATE DATABASE blog_forteroche
			->     CHARACTER SET utf8mb4
			->     COLLATE utf8mb4_unicode_ci;


	- Activation de la libraire intl (php.ini)
	
	- Renomme config/_config.php -> config/config.php.

1. Initialialisation du projet

	Et :
		- Création repository github "oc-3-2-blog-forteroche"
		
	git init
	git add .
	git commit -m "Initialisation du projet P3L2 - Blog Forteroche"	
	git remote add origin git@github.com:roogido/oc-3-2-blog-forteroche.git
	git push -u origin main

		
2. Stocker les informations manquantes
	- Ajout de la colonne views (int non signé) dans table article :
		ALTER TABLE article
			ADD COLUMN views INT UNSIGNED NOT NULL DEFAULT 0 AFTER content;

	- Adaptation de l'entité : classe models/Article
		- Attribut + setter/getter

	- Ajout de la méthode incrementViews au modèle  models/ArticleManager

3.

4.

5.


6.
